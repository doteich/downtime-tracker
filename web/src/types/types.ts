export interface Event {
    id: number;
    name: string;
    startDate: string;
    endDate: string;
    location: string;
    color: string;
}
export interface Location {
    id: number;
    name: string;
    color: string;
}

export interface DownTimeType {
    id: number;
    name: string;
    color: string;
}

